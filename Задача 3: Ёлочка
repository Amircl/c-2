#include <stdio.h>

int main() {
    int X, i, j, k;

    printf("Введите количество уровней ёлки: ");
    scanf("%d", &X);

    for (i = 1; i <= X; i++) {
    
        for (j = 1; j <= X - i; j++) {
            printf(" ");
        }
      
        for (k = 1; k <= 2 * i - 1; k++) {
            printf("*");
        }
        printf("\n");
    }

    for (i = 1; i <= X - 1; i++) {
        printf(" ");
    }
    printf("|\n");

    return 0;
}
